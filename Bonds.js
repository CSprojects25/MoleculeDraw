class Bonds {
    constructor(index, bondType) {
        this.index = index; // index (of the main array) that the atom it is connected to
        this.bondType = bondType; // single, double, triple (1, 2, 3)
    }
}